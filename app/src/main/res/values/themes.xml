<resources>
    <!-- Base application theme. -->
    <style name="Theme.NewsFeeds" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/color_on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/color_on_secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_primary_variant</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@color/color_window_background</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="WrapContentDialogTheme" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="windowMinWidthMajor">0%</item>
        <item name="windowMinWidthMinor">0%</item>
    </style>
</resources>